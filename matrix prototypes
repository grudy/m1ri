//


/*
int columns(vbg ** matrix)
{
    int  ncolumns = sizeof(vbg);
    return ncolumns;
}
*//*
vbg ** creatematrix(int rblks,int  cblks)   //row blocks, column blocks{
{
    vbg ** matrixn;
    matrixn =  calloc(  rblks, (sizeof(vbg *)));
    
    //matrixn = malloc(rblks   *  sizeof(vbg *));// * 2 for each member of the struct, sign and units
    
    
    for(int i = 0; i < rblks; i++)
        
    {
        matrixn[i] = calloc(cblks,   (sizeof(vbg)));
        
        
        
    }
    
    
    
    
    return matrixn;
    
}


*/
